.build/

# Workstream 3 reads a recording supplied through ECHOTYPE_FIXTURE_WAV. Recordings and
# transcripts are evidence, never artefacts: they are not committed.
*.wav
Tests/Fixtures/

# The live session's recorded event sequence is evidence too, and the documented command
# writes it by hand. It is never committed.
*.log
